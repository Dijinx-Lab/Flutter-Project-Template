part of 'constants.dart';

enum AppTheme {
  light,
  dark,
}

enum ButtonTypes {
  primary,
  secondary,
  danger,
  textOnly,
}

enum OverlayLoaderTypes {
  circular,
  line,
}

enum Spacings {
  form,
  pageTop,
  xSmall,
  small,
  medium,
  large,
  xLarge,
  xxLarge,
}

enum FontSizes {
  title,
  titleBold,
  heading,
  headingBold,
  bodyLarge,
  bodyLargeBold,
  body,
  bodyBold,
  label,
  labelBold,
}

enum ComponentStyles {
  adaptive,
  material,
  cupertino,
}

enum ShadowStyles {
  card,
}
